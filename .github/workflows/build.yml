name: build

on:
  push:

jobs:
  test:
      runs-on: macos-latest
      steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Make
        run: make

      - name: Run tests
        run: make unit-tests
